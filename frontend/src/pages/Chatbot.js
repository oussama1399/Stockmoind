import React from 'react';
import { Typography, Box } from '@mui/material';

const Chatbot = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        AI Chatbot Assistant
      </Typography>
      <Typography>
        Intelligent chatbot for querying stock levels and generating reports will be implemented here.
      </Typography>
    </Box>
  );
};

export default Chatbot;