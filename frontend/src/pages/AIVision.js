import React from 'react';
import { Typography, Box } from '@mui/material';

const AIVision = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        AI Vision
      </Typography>
      <Typography>
        Computer vision features for automatic product counting and detection will be implemented here.
      </Typography>
    </Box>
  );
};

export default AIVision;